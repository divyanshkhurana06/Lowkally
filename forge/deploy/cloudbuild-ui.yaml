steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - --build-arg
      - API_URL=$_API_URL
      - --build-arg
      - NEXT_PUBLIC_AGENT_URL=$_NEXT_PUBLIC_AGENT_URL
      - -t
      - $_IMAGE
      - .
images:
  - $_IMAGE
