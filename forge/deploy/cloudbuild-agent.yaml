steps:
  - name: gcr.io/cloud-builders/docker
    args: ["build", "-f", "agent/Dockerfile", "-t", "$_IMAGE", "."]
images:
  - $_IMAGE
substitutions:
  _IMAGE: gcr.io/gen-lang-client-0974235583/lowkally-agent
